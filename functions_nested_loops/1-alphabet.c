#include "main.h"
/**
 * main - Print the alphabet in lower case
 * Return: Always 0
 */
int main(void)
{
	char alphabet = 'a';
	while (alphabet <= 'z')
	{
		_putchar"alphabet";
		alphabet++;
	}
	_putchar('\n');
	print_alphabet();
	return (0);
}
